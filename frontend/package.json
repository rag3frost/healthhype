{
  "name": "medical-prediction-frontend",
  "version": "1.0.0",
  "description": "Frontend for medical prediction system",
  "scripts": {
    "dev": "live-server --port=3000 --host=127.0.0.1 --no-browser",
    "start": "live-server --port=3000 --host=127.0.0.1 --no-browser"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  },
  "dependencies": {
    "maplibre-gl": "^4.7.1",
    "ogl": "^1.0.11",
    "radar-sdk-js": "^4.4.10"
  }
}
